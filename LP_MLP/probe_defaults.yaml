num_runs: 5
base_seed: 23
seed_stride: 1
base_split_index: 1
modes:
  - linear_probe
  - mlp_probe
mlp_probe:
  hidden_dim: 64
  dropout: 0.1
  num_layers: 2
  input_norm: layernorm
  batch_size: 128
  epochs: 200
  max_patience: 20
  lr: 0.001
  weight_decay: 0.0001

