1、复制粘贴文件夹📂：
cp -r /data/ningzh/lcm_ds/outputs /mnt/dataset1/ningzh/

如果你现在就在 /data/ningzh/lcm_ds 这个目录里，也可以写成：
cp -r outputs /mnt/dataset1/ningzh/


'''
mkdir -p /mnt/dataset3/nzh/outputs_1 && cp \
outputs/table3_style_summary.csv \
outputs/task_summary.csv \
/mnt/dataset3/nzh/outputs_1/
'''


2、更新repo：

可以，服务器上就用 git pull，不用重新克隆。
如果你现在就在 /data/ningzh/lcm_ds 这个仓库里，最简单流程是：
git branch --show-current
git status
git pull
///
git pull origin omni_table3_10ds

3/